# OAuth 2.0 & OIDC

OAuth is a protocal (communication pattern) for delegating authorization. It allows a third party application
to access resources on behalf of a user.

OIDC is essentially just a standardisation of the 'user' resource. Which allows for delegated authentication.
